{
  "deps": [],
  "tests": [],
  "external": {
    "ref": "ghcr.io/hwaien/devcontainer-features/match-host-time-zone",
    "description": "Bind-mounts the host's /etc/localtime into the container so its clock/timezone matches the host. Linux hosts only (Docker Desktop's VM has its own /etc/localtime, not the Mac/Windows host's).",
    "options": {}
  }
}
