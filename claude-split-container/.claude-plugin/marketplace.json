{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "devcontainer-config",
  "description": "Plugins from the devcontainer-config repo. Named after the repo rather than this directory so the plugin id reads claude-split-container@devcontainer-config, not the same word twice.",
  "owner": {
    "name": "Dominique Unruh"
  },
  "plugins": [
    {
      "name": "claude-split-container",
      "description": "Split host/container working style: a free devcontainer (run_bash_container) sharing the project dir is the default for shell work; the host is reachable only through the built-in Bash tool, gated by a hook.",
      "source": "./",
      "category": "productivity"
    }
  ]
}
